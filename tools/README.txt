A couple of tools to use as standalone, not as a function.

'txt_concatenate' can be used to combine multiple txt files with data from LinoSPAD2.
As the maximum number of cycles is 11999 with 1-512 lines of data per pixel read out
and 256 pixels total there is not much data for sufficient statistics.